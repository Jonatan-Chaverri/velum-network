{
  "name": "@velum/sdk",
  "version": "0.1.0",
  "description": "Velum Network SDK — confidential agent-to-agent payments on Arbitrum",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "example": "tsx --env-file-if-exists=.env examples/buy-research.ts"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.10.1",
    "express": "^4.18.2",
    "tsx": "^4.7.0",
    "typescript": "^5.7.2"
  }
}
